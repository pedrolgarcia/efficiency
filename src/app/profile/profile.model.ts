export class Profile {
    constructor(
        id: number,
        name: string,
        email: string,
        password?: string,
        avatar?: string,
        created_at?: string,
        updated_at?: string
    ) {}
}